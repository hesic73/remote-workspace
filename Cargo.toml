[workspace]
members = [
    "crates/remote-workspace-protocol",
    "crates/remote-workspace-server",
    "crates/remote-workspace-client",
    "crates/remote-workspace-mcp",
]
resolver = "2"

[workspace.package]
version = "0.4.0"
edition = "2021"
license = "MIT"

[workspace.dependencies]
remote-workspace-protocol = { path = "crates/remote-workspace-protocol" }
remote-workspace-client = { path = "crates/remote-workspace-client" }

tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
thiserror = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
sha2 = "0.10"
tempfile = "3"
toml = "0.8"
anyhow = "1"
hex = "0.4"
rmcp = { version = "1", features = ["server", "transport-io"] }
schemars = "1"

[profile.release]
lto = "thin"
